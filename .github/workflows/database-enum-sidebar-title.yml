name: Database enum sidebarTitle auto-fix

on:
  pull_request:
    branches: [main]
    paths:
      - 'en/database/tables/enums/**'

# Automatically cancel in-progress runs on the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  add-sidebar-title:
    name: "Add sidebarTitle to changed enum pages"
    runs-on: ubuntu-latest
    # A fork PR's default token can't push back to the fork - skip cleanly
    # rather than fail loudly. Same-repo branches (the normal case for this
    # ADO-pipeline-generated tree) proceed as usual.
    if: github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - name: Checkout PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Get changed files
        id: changed
        uses: tj-actions/changed-files@v45
        with:
          files: |
            en/database/tables/enums/**/*.md
            en/database/tables/enums/**/*.mdx

      - name: Set up Python
        if: steps.changed.outputs.any_changed == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Add missing sidebarTitle to changed enum pages
        if: steps.changed.outputs.any_changed == 'true'
        env:
          CHANGED_FILES: ${{ steps.changed.outputs.all_changed_files }}
        run: python tools/ci/add-database-enum-sidebar-titles.py --apply $CHANGED_FILES

      - name: Commit and push fix, if any
        if: steps.changed.outputs.any_changed == 'true'
        env:
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: |
          if git diff --quiet; then
            echo "No sidebarTitle changes needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add en/database/tables/enums
          git commit -m "Add sidebarTitle to database-reference enum pages (#260)"
          git push origin "HEAD:$HEAD_REF"
