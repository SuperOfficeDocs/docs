---
uid: crmscript.netserver.nscustomerlanguage
title: NSCustomerLanguage
description: "Customer languages that customer center supports. Carrier object for CustomerLanguage."
keywords: CRMScript, NSCustomerLanguage, API reference
author: SuperOffice Product and Engineering
date: 01.08.2026
version: 11.7
content_type: reference
language: en
---

# NSCustomerLanguage

Customer languages that customer center supports. Carrier object for CustomerLanguage.

## Constructors

### NSCustomerLanguage()

```crmscript
NSCustomerLanguage
```

Initializes a new instance of the NSCustomerLanguage class.

## Methods

## GetId()

Gets the primary key (ID) for the NSCustomerLanguage.

```crmscript
Integer GetId()
```

**Returns:** [CRMScript.Global.Integer](CRMScript.Global.Integer)

**Example:**

```crmscript
NSCustomerLanguage thing;
Integer id = thing.GetId();
```

## GetTooltip()

Gets the tooltip description for the NSCustomerLanguage list item.

```crmscript
String GetTooltip()
```

**Returns:** [CRMScript.Global.String](CRMScript.Global.String)

**Example:**

```crmscript
NSCustomerLanguage thing;
String descr = thing.GetTooltip();
```

## GetValue()

Gets the name of the NSCustomerLanguage.

```crmscript
String GetValue()
```

**Returns:** [CRMScript.Global.String](CRMScript.Global.String)

**Example:**

```crmscript
NSCustomerLanguage thing;
String name = thing.GetValue();
```

## SetId(Integer)

Sets the primary key (ID) for the NSCustomerLanguage

```crmscript
Void SetId(Integer id)
```

**Returns:** [CRMScript.Global.Void](CRMScript.Global.Void)

**Example:**

```crmscript
NSCustomerLanguage thing;
thing.SetId(123);
```

## SetTooltip(String)

Sets the tooltip description for the NSCustomerLanguage list item.

```crmscript
Void SetTooltip(String tooltip)
```

**Returns:** [CRMScript.Global.Void](CRMScript.Global.Void)

**Example:**

```crmscript
NSCustomerLanguage thing;
String descr = "something new";
thing.SetTooltip(descr);
```

## SetValue(String)

Sets the name of the NSCustomerLanguage item.

```crmscript
Void SetValue(String value)
```

**Returns:** [CRMScript.Global.Void](CRMScript.Global.Void)

**Example:**

```crmscript
NSCustomerLanguage thing;
thing.SetValue("frotz");
```
