---
uid: crmscript.netserver.nsentityelement
title: NSEntityElement
description: "Generic carrier object for entity elements such as Phone, EntityElement, Url. These elements do not have id and rank. These values are changed on each save, and should not be depended on. When used in a list or array the rank is the ascending list order. Carrier object for EntityElement."
keywords: CRMScript, NSEntityElement, API reference
author: SuperOffice Product and Engineering
date: 01.08.2026
version: 11.7
content_type: reference
language: en
---

# NSEntityElement

Generic carrier object for entity elements such as Phone, EntityElement, Url. These elements do not have id and rank. These values are changed on each save, and should not be depended on. When used in a list or array the rank is the ascending list order. Carrier object for EntityElement.

## Constructors

### NSEntityElement()

```crmscript
NSEntityElement
```

Initializes a new instance of the NSEntityElement class.

## Methods

## GetDescription()

```crmscript
String GetDescription()
```

**Returns:** [CRMScript.Global.String](CRMScript.Global.String)

## GetStrippedValue()

```crmscript
String GetStrippedValue()
```

**Returns:** [CRMScript.Global.String](CRMScript.Global.String)

## GetValue()

```crmscript
String GetValue()
```

**Returns:** [CRMScript.Global.String](CRMScript.Global.String)

## SetDescription(String)

```crmscript
Void SetDescription(String description)
```

**Returns:** [CRMScript.Global.Void](CRMScript.Global.Void)

## SetStrippedValue(String)

```crmscript
Void SetStrippedValue(String value)
```

**Returns:** [CRMScript.Global.Void](CRMScript.Global.Void)

## SetValue(String)

```crmscript
Void SetValue(String value)
```

**Returns:** [CRMScript.Global.Void](CRMScript.Global.Void)
