---
title: "FormFolders"
description: "Archive of form folders. Lists all rows of s_picture_folder where folder_type = formFolder. Mirrors `MailingFolderProvider`. Named in the plural — `SuperOffice.CRM.Lists.FormFolderProvider` is the MDO list that backs the folder pickers, and several files in this assembly import both namespaces."
keywords: FormFolders archive provider
generated: true
content_type: reference
envir: onsite, online
---

This provider name is implemented by the class `SuperOffice.CRM.ArchiveLists.FormFoldersProvider` inside NetServer's SODatabase assembly.

Archive of form folders. Lists all rows of s_picture_folder where
folder_type = formFolder. Mirrors `MailingFolderProvider`.

Named in the plural — `SuperOffice.CRM.Lists.FormFolderProvider` is the
MDO list that backs the folder pickers, and several files in this assembly
import both namespaces.

## Supported Entities
| Name | Description |
| ---- | ----- |
|"formfolder"|formfolder|

## Supported Columns
| Name | Restriction | Description | OrderBy |
| ---- | ----- | ------- | ------ |
|folderId|int|Hierarchy ID: Foreign key to hierarchy table| x |
|folderName|string|Hierarchy name: The full name/path from table hierarchy| x |
|formThumbnail| *None* |Thumbnail: Form thumbnail|  |
|getAllRows|bool|GetAll: Get all rows of archive - use with care, you may be fetching the whole database|  |
|getNoRows|bool|GetNone: Do not get any rows from the archive|  |
|parentFolderId|int|Hierarchy ID: Foreign key to hierarchy table| x |
|sortGroup| *None* || x |
|sortName| *None* || x |
|type| *None* |Type: Displays the type of an activity|  |

## Sample

```http!
GET /api/v1/archive/FormFolders?$select=folderName,formThumbnail,type
Authorization: Basic dGplMDpUamUw
Accept: application/json; charset=utf-8
Accept-Language: sv

```

See also: `IArchiveAgent`.

