# Personal AI configuration (private repo, not for all contributors)
ai-agents/
AGENTS.md
.github/copilot-instructions.md

# Temporary: per-folder markdownlint overrides showing up during the Mintlify
# conversion pipeline (e.g. contribute/includes/.markdownlint.yml) - ignored
# until there's a real decision on whether/how these should be committed.
**/.markdownlint.yml

# Python bytecode cache (tools/*.py scripts)
__pycache__/

# Vale style packages downloaded via `vale sync` (see .vale.ini Packages=)
# — third-party content, not ours to commit. Run `vale sync` after cloning.
vale/Microsoft/
vale/.vale-config/
