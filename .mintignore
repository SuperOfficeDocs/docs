# Exclude draft documents
drafts/
*.draft.mdx

# Exclude internal documentation
internal/

# Exclude docfx assets
**/toc.yml
**/index.yml

# Exclude all API source files (used for generation)
api-sources/

# Exclude transformation scripts
tools/
transform-*.ps1

# Exclude specific files
private-notes.md
bergfrid-tmp/
release-notes/poc
redirects.json

# Exclude selected content folders to keep mint dev below 5000 pages
### Do not commit changes to this section without approval ###

# Ignore translations except index files
#de/**
#!de/**/
#!de/**/index.md
#!de/**/index.mdx
#no/**
#!no/**/
#!no/**/index.md
#!no/**/index.mdx
#nl/**
#!nl/**/
#!nl/**/index.md
#!nl/**/index.mdx
#sv/**
#!sv/**/
#!sv/**/index.md
#!sv/**/index.mdx

# Newly forklifted from superoffice-docs (2026-07-13), not yet reformatted for Mintlify
# (raw DocFx md/mdx, will break mint dev) — blanket-ignored for now, no exceptions.
# de, no, sv, and nl are deliberately NOT ignored (converted to Mintlify, excluded from this batch). downloads is also NOT ignored — its one real content file (api/index.md) is converted; the other 84 files are static assets, not pages. media is static assets, not pages.
da/**
integrations/**
release-notes/**

# Ignore database except index files
en/database/**
!en/database/**/
!en/database/**/index.md
!en/database/**/index.mdx

# Ignore all reference folders except index files (but not agents-webapi reference with OpenAPI specs)
#**/reference/**
#!**/reference/**/

# Ignore SOAP reference
en/api/web-services/endpoints/soap/reference/**
!**/reference/**/index.md
!**/reference/**/index.mdx

# Ignore CRMScript reference
en/customization/crmscript/reference/**